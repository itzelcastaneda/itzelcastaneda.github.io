# Itzel Castañeda — Jekyll Site (Gaudí-inspired)

This repository scaffold was generated automatically. It includes a custom, Gaudí-esque theme and sample content tailored for Itzel Castañeda (Master of Computer Science student at Rice University).

## How to use

1. Unzip the archive to a folder named `itzelcastaneda.github.io` (or push the files to a GitHub repo with that name).
2. Commit and push to GitHub. In the repository Settings → Pages, select `main` branch and root for publishing (if needed).
3. Wait a minute and visit `https://<your-username>.github.io` (or the URL configured in _config.yml).

## Replace placeholders

- Update the social links in `_includes/footer.html`.
- Add your project pages and real images in `assets/images/`.
- Customize `_config.yml` (url, baseurl, analytics ID).

Enjoy! — Generated for Itzel Castañeda
